{
  "name": "certquest-api-image-upload",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "clean": "rimraf dist",
    "prestart": "npm run clean && npm run build",
    "start": "func start",
    "test": "echo \"No tests yet...\""
  },
  "dependencies": {
    "@azure/functions": "^4.0.0",
    "@azure/storage-blob": "^12.26.0",
    "certquest-api-image-upload": "file:",
    "uuidv4": "^6.2.13"
  },
  "devDependencies": {
    "@types/node": "^20.x",
    "@types/uuidv4": "^2.0.0",
    "rimraf": "^5.0.0",
    "typescript": "^4.0.0"
  },
  "main": "dist/src/{index.js,functions/*.js}"
}
